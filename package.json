{
  "name": "voip-push-tester",
  "version": "0.0.1",
  "description": "This is a library that uses Twilio to automatically make calls and check if VoIP Push notifications for mobile apps have been received.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "DEV=true ts-node src/index.ts",
    "dryrun": "DRYRUN=true pnpm dev",
    "build": "tsc"
  },
  "keywords": [],
  "author": "iori-kanno",
  "license": "MIT",
  "dependencies": {
    "@slack/web-api": "^6.8.1",
    "twilio": "^4.11.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": "please use pnpm",
    "yarn": "please use pnpm"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
